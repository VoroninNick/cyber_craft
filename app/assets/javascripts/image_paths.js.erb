<%
 def quoted_asset_path(filename)
    '"' + asset_path(filename) + '"'
 end
 %>
if(!window.images) {
    window.images = {}
}

images['form_preloader'] = <%= quoted_asset_path("loader_1.gif") %>
images['logo_png'] = <%= quoted_asset_path("logo.png") %>